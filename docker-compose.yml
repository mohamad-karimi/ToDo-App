services:
  backend:
    build: .
    container_name: todoapp-backend

    env_file:
      - .env

    ports:
      - "8000:8000"
    volumes:
      - ./core:/app
    command: python manage.py runserver 0.0.0.0:8000