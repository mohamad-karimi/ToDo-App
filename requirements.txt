# Base requirements. Wasmer/Anybuild installs ONLY this file, for the
# wasix_wasm32 platform with --only-binary=:all:, so it must not contain
# packages that have no WASIX wheel (e.g. the PostgreSQL driver).
# Local / Docker installs use requirements-docker.txt, which adds the
# PostgreSQL driver on top of this file.

# Django
Django==5.2.14

# Django REST Framework
djangorestframework==3.18.0
django-filter==26.1

# JWT Authentication
djangorestframework_simplejwt==5.5.1

# API Documentation
drf-spectacular==0.30.0
drf-spectacular-sidecar==2026.8.1

# Environment Variables
python-decouple==3.8

# Testing
pytest
pytest-django

# Code Quality
flake8
black

# Make fake data
Faker

# Background process and cache
celery
redis
django-celery-beat
django-redis
requests

# Deployment
gunicorn
uvicorn
